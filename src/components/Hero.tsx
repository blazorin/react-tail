import React from 'react';
import exp from 'node:constants';

const Hero = (props: any) => {
	return (
		<section className={'bg-color-main opacity-75 wide:padding-r padding-b'}>
			<div>d</div>
		</section>
	);
};

export default Hero;
